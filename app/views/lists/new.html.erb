<body>
  <div class="container">
    <h1>New List</h1>

    <%= simple_form_for (@list) do |form| %>
      <%= form.input :name %>
      <%= form.input :photo, as: :file %>
      <%= form.button :submit %>
    <% end %>
    <%= link_to("Back", lists_path) %>
  </div>
</body>
